PORT=3000
NODE_ENV=development

# Database configuration (Leave DB_HOST empty or SQLite to use SQLite file DB locally)
DB_DIALECT=mariadb
DB_HOST=mariadb
DB_PORT=3306
DB_NAME=e_utilities_cost
DB_USER=app_user
DB_PASSWORD=changeme

# JWT Configuration
JWT_SECRET=e_utilities_cost_super_secret_jwt_key_2026
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d
